#include <bits/stdc++.h>
using namespace std;

int kaing(int N, int M, int x, int y)
{
    int result;

    return result;
}

int main()
{
    int T, N, M, x, y;
    cin >> T;
    for (int i = 0; i < T; i++)
    {
        cin >> N >> M >> x >> y;
        cout << kaing(N, M, x, y);
    }
}
